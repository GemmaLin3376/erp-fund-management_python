<!--
版本信息
==========
版本号: V1.2
生成时间: 2026-04-01 11:57:00
变更描述: 新增列表复选框交互规范（双向同步）
变更人: 开发团队
-->

# ERP资金管理系统 需求文档（最终版）

> 版本：v1.0（已交付实现版）  
> 项目：arPython  
> 技术栈：Python / Flask / SQLite / Bootstrap

---

## 一、系统概述

基础ERP资金管理系统，覆盖资金收支的基础资料维护和应收应付单据管理，支持单据完整生命周期操作（新增、编辑、复制、审核、反审核、删除）及批量操作，并实现跨单据的金额自动反写联动。

---

## 二、前端布局规范

### 2.1 导航结构

采用**左侧竖排布局**，一级菜单分为两大分类：

```
├── 基础资料
│   ├── 客户管理
│   ├── 收入类别管理
│   └── 收款账户管理
└── 应收应付
    ├── 其他收入单
    └── 收款单
```

### 2.2 页面结构

每个模块包含：
- **列表页**：含搜索、批量操作、数据表格
- **新增/编辑页（表单页）**：含表头信息 + 分录明细表
- **详情页**：含操作按钮区（card-header）+ 单据信息 + 分录信息

---

## 三、基础资料模块

### 3.1 客户管理

| 功能 | 说明 |
|------|------|
| 新增 | 输入名称，编码自动生成（`KH` + 3位数字，如 `KH001`） |
| 编辑 | 修改名称 |
| 删除 | 被其他收入单或收款单引用后**禁止删除** |
| 查询 | 支持按名称/编码关键字搜索 |
| 预置数据 | 系统初始化时内置示例客户数据 |

### 3.2 收入类别管理

| 功能 | 说明 |
|------|------|
| 新增 | 输入名称，编码自动生成（`SR` + 3位数字，如 `SR001`） |
| 编辑 | 修改名称 |
| 删除 | 被其他收入单分录引用后**禁止删除** |
| 查询 | 支持关键字搜索 |
| 预置数据 | 系统初始化时内置示例收入类别 |

### 3.3 收款账户管理

| 功能 | 说明 |
|------|------|
| 新增 | 输入名称，编码自动生成（`ZH` + 3位数字，如 `ZH001`） |
| 编辑 | 修改名称 |
| 删除 | 被收款单引用后**禁止删除** |
| 查询 | 支持关键字搜索 |
| 预置数据 | 系统初始化时内置示例账户 |

---

## 四、应收应付模块

### 4.1 其他收入单

#### 4.1.1 单据字段

**表头信息**：

| 字段 | 说明 |
|------|------|
| 单据编号 | 自动生成（`QTSR` + 3位数字，如 `QTSR001`） |
| 单据日期 | 手动选择 |
| 客户名称 | 从基础资料选择 |
| 单据总金额 | 所有分录金额合计（自动计算） |

**分录信息**（多行）：

| 字段 | 说明 |
|------|------|
| 收入类别 | 从基础资料选择 |
| 金额 | 手动输入，必须大于0 |
| 备注 | 可选 |

#### 4.1.2 操作规范

| 操作 | 条件 | 触发位置 |
|------|------|---------|
| 新增 | 无限制 | 列表页按钮 |
| 编辑 | 仅**未审核**状态 | 列表页行操作 + 详情页 |
| 复制 | 任意状态 | 列表页行操作 + 详情页 |
| 审核 | 仅**未审核**状态 | 列表页行操作 + 详情页 |
| 反审核 | 仅**已审核**状态，且**无任何关联收款单** | 列表页行操作 + 详情页 |
| 删除（单个） | 仅**未审核**状态 | 列表页行操作 + 详情页 |
| 批量审核 | 批量，仅对未审核单据生效 | 列表页批量操作栏 |
| 批量反审核 | 批量，仅对已审核且无收款单的单据生效 | 列表页批量操作栏 |
| 批量删除 | 批量，仅对未审核单据生效 | 列表页批量操作栏 |

> **注意**：批量操作**不含复制**。

#### 4.1.3 列表展示字段与顺序

```
复选框 → 审核状态 → 单据编号 → 单据日期 → 客户名称 → 单据总金额 → 已收款金额 → 未收款金额 → 收款状态 → 操作
```

**审核状态徽章颜色**：

| 状态 | 颜色 | 样式 |
|------|------|------|
| 未审核 | 黄色 | `badge-warning` |
| 已审核 | 绿色 | `badge-success` |

**收款状态徽章颜色**：

| 收款状态 | 颜色 | 样式 | 显示条件 |
|---------|------|------|---------|
| —（灰色横线） | 灰色 | `badge-secondary` | 单据未审核时 |
| 未收款 | 蓝色 | `badge-info` | 已审核，已收款=0 |
| 部分收款 | 黄色 | `badge-warning` | 0 < 已收款 < 总金额 |
| 全部收款 | 绿色 | `badge-success` | 已收款 ≥ 总金额 |

#### 4.1.4 列表页行操作按钮规则

| 单据状态 | 显示按钮 |
|---------|---------|
| 未审核 | 复制、编辑、审核、删除 |
| 已审核 | 复制、反审核 |

#### 4.1.5 详情页操作按钮规则（card-header 区域）

| 单据状态 | 显示按钮 |
|---------|---------|
| 未审核 | 复制、编辑、审核、删除、返回 |
| 已审核 | 复制、反审核、返回 |

#### 4.1.6 反审核前置校验规则

执行反审核前必须校验：
- 该收入单是否存在**任意状态**的关联收款单（包含未审核收款单）
- 若存在 → 拒绝反审核，提示"存在关联收款单，不支持反审核"

---

### 4.2 收款单

#### 4.2.1 单据字段

**表头信息**：

| 字段 | 说明 |
|------|------|
| 单据编号 | 自动生成（`SK` + 3位数字，如 `SK001`） |
| 单据日期 | 手动选择 |
| 客户名称 | 从基础资料选择 |
| 收款账户 | 从基础资料选择 |
| 单据总金额 | 所有分录金额合计（自动计算） |

**分录信息**（多行）：

| 字段 | 说明 |
|------|------|
| 关联收入单 | 从弹窗中选择已审核且未全部收款的其他收入单 |
| 收款金额 | 手动输入，必须大于0，且不超过该收入单的未收款金额 |
| 备注 | 可选 |

**分录表列顺序**：关联收入单 → 收款金额 → 备注

#### 4.2.2 操作规范

| 操作 | 条件 | 触发位置 |
|------|------|---------|
| 新增 | 无限制 | 列表页按钮 |
| 编辑 | 仅**未审核**状态 | 列表页行操作 + 详情页 |
| 审核 | 仅**未审核**状态 | 列表页行操作 + 详情页 |
| 反审核 | 仅**已审核**状态 | 列表页行操作 + 详情页 |
| 删除（单个） | 仅**未审核**状态 | 列表页行操作 + 详情页 |
| 批量审核 | 批量，仅对未审核单据生效 | 列表页批量操作栏 |
| 批量反审核 | 批量，仅对已审核单据生效 | 列表页批量操作栏 |
| 批量删除 | 批量，仅对未审核单据生效 | 列表页批量操作栏 |

> **注意**：收款单**不支持复制**（列表页和详情页均无复制按钮）。

#### 4.2.3 列表展示字段与顺序

```
复选框 → 单据状态 → 单据编号 → 单据日期 → 客户名称 → 收款账户 → 单据总金额 → 操作
```

#### 4.2.4 列表页行操作按钮规则

| 单据状态 | 显示按钮 |
|---------|---------|
| 未审核 | 编辑、审核、删除（无复制） |
| 已审核 | 反审核（无复制） |

#### 4.2.5 详情页操作按钮规则（card-header 区域）

| 单据状态 | 显示按钮 |
|---------|---------|
| 未审核 | 编辑、审核、删除、返回（无复制） |
| 已审核 | 反审核、返回 |

#### 4.2.6 选择源单弹窗规范

- 弹窗模式，支持 Tab 切换（当前仅"其他收入单"一个 Tab，预留扩展）
- 支持按客户过滤（与收款单表头选择的客户联动）
- 支持搜索、多选、全选
- **过滤规则**：只展示"已审核"且"未收款金额 > 0"的其他收入单
- **展示字段**与其他收入单列表保持一致：审核状态、单据编号、单据日期、客户名称、单据总金额、已收款金额、未收款金额、收款状态

---

## 五、核心业务规则

### 5.1 金额反写规则

收款单的**保存（新增/编辑）**和**删除**操作，必须实时更新关联的其他收入单的以下字段：

- `已收款金额`：所有关联该收入单的收款单分录金额之和（含所有状态）
- `未收款金额`：单据总金额 - 已收款金额
- `收款状态`：根据下方收款状态判定规则自动计算

> **重要**：收款单的**审核/反审核**操作**不触发**其他收入单的状态更新，仅变更收款单自身状态。

### 5.2 收款状态判定规则

```
已收款金额 >= 单据总金额  →  全部收款
已收款金额 > 0           →  部分收款
已收款金额 = 0           →  未收款（仅适用于已审核收入单）
单据未审核               →  显示"—"（灰色）
```

### 5.3 编辑收款单的金额处理规则

编辑收款单时，必须执行以下两步：
1. **先回退**：将旧分录关联收入单的已收款金额减去旧分录金额
2. **再累加**：将新分录金额累加到关联收入单的已收款金额

两步缺一不可，确保已收款金额始终等于新金额，而非旧金额+新金额的叠加。

### 5.4 编码自动生成规则

| 模块 | 前缀 | 格式示例 |
|------|------|---------|
| 客户 | `KH` | `KH001`、`KH002` |
| 收入类别 | `SR` | `SR001`、`SR002` |
| 收款账户 | `ZH` | `ZH001`、`ZH002` |
| 其他收入单 | `QTSR` | `QTSR001`、`QTSR002` |
| 收款单 | `SK` | `SK001`、`SK002` |

### 5.5 金额合法性校验规则

| 场景 | 校验规则 | 提示信息 |
|------|---------|---------|
| 分录金额未填写 | `amount is None` | 分录信息不完整 |
| 分录金额 ≤ 0 | `amount <= 0` | 金额必须大于0 |
| 收款金额超过未收款金额 | `amount > unreceived_amount` | 收款金额不能超过未收款金额 |

> **注意**：不可使用 `if not amount` 作为判断，因为 Python 中 `not 0 == True`，会将合法的 `0` 误判为"未填写"。

### 5.6 基础资料引用保护规则

| 基础资料 | 引用检查 | 被引用时操作 |
|---------|---------|------------|
| 客户 | 被其他收入单或收款单引用 | 禁止删除 |
| 收入类别 | 被其他收入单分录引用 | 禁止删除 |
| 收款账户 | 被收款单引用 | 禁止删除 |

### 5.7 单据状态转换规则

**其他收入单**：

```
未审核 --[审核]--> 已审核 --[反审核（无关联收款单）]--> 未审核
```

**收款单**：

```
未审核 --[审核]--> 已审核 --[反审核]--> 未审核
```

---

## 六、接口规范

### 6.1 选择源单 API

```
GET /income_order/api/available_orders?customer_id={id}
```

**返回字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 收入单ID |
| code | string | 单据编号 |
| customer_name | string | 客户名称 |
| order_date | string | 单据日期 |
| total_amount | decimal | 单据总金额 |
| received_amount | decimal | 已收款金额 |
| unreceived_amount | decimal | 未收款金额 |
| audit_status | string | 审核状态（已审核） |
| receipt_status | string | 收款状态 |

**过滤条件**：仅返回 `status = 已审核` 且 `unreceived_amount > 0` 的记录。

### 6.2 未收款金额查询 API

```
GET /income_order/api/{id}/unreceived_amount
```

**返回字段**：`total_amount`、`received_amount`、`unreceived_amount`

---

## 七、UI规范汇总

### 7.1 按钮显示判定表

| 页面 | 未审核状态 | 已审核状态 |
|------|-----------|-----------|
| 其他收入单列表（行操作） | 复制、编辑、审核、删除 | 复制、反审核 |
| 收款单列表（行操作） | 编辑、审核、删除 | 反审核 |
| 其他收入单详情（card-header） | 复制、编辑、审核、删除、返回 | 复制、反审核、返回 |
| 收款单详情（card-header） | 编辑、审核、删除、返回 | 反审核、返回 |

### 7.2 列表字段顺序

**其他收入单列表**：
```
复选框 → 审核状态 → 单据编号 → 单据日期 → 客户名称 → 单据总金额 → 已收款金额 → 未收款金额 → 收款状态 → 操作
```

**收款单列表**：
```
复选框 → 单据状态 → 单据编号 → 单据日期 → 客户名称 → 收款账户 → 单据总金额 → 操作
```

**收款单分录表**：
```
关联收入单 → 收款金额 → 备注
```

### 7.3 徽章颜色规范

| 状态 | 颜色 | Bootstrap类 |
|------|------|------------|
| 未审核（审核状态列） | 黄色 | `badge-warning` |
| 已审核（审核状态列） | 绿色 | `badge-success` |
| —（未审核时收款状态列） | 灰色 | `badge-secondary` |
| 未收款 | 蓝色 | `badge-info` |
| 部分收款 | 黄色 | `badge-warning` |
| 全部收款 | 绿色 | `badge-success` |

---

## 八、批量操作规范

### 8.1 其他收入单批量操作

| 操作 | 说明 |
|------|------|
| 批量审核 | 仅对未审核单据生效，已审核单据计入失败数 |
| 批量反审核 | 仅对已审核且无收款单关联的单据生效 |
| 批量删除 | 仅对未审核单据生效 |
| 批量复制 | **不支持** |

### 8.2 收款单批量操作

| 操作 | 说明 |
|------|------|
| 批量审核 | 仅对未审核单据生效 |
| 批量反审核 | 仅对已审核单据生效 |
| 批量删除 | 仅对未审核单据生效，**必须调用 `order.delete()` 方法**（确保关联收入单金额回退） |

### 8.3 批量操作通用规范

- 未传入 IDs 或 IDs 为空列表时返回失败
- 操作完成后返回 `{"success": true, "message": "操作成功 N 条，失败 M 条"}` 格式消息
- 部分成功时仍返回 `success: true`，但消息中明确失败数量
- **批量反审核失败提示**：失败消息需包含单号及具体原因，格式为 `{单号}：{失败原因}`
  - 示例：`QTSR003：该单据已关联收款单，不能反审核`
  - 示例：`QTSR004：当前状态为未审核，不能反审核`

### 8.4 列表复选框交互规范

- **行复选框点击**：
  - 切换当前行选中状态（高亮/取消高亮）
  - 同步更新全选框状态（所有行选中时勾选，否则取消）
  - 更新已选数量显示
  
- **全选框点击**：
  - 切换所有行复选框状态（全选/全取消）
  - 同步更新所有行高亮状态
  - 更新已选数量显示

---

## 九、预置数据规范

系统启动时自动初始化以下基础数据：

| 类别 | 编码 | 名称 |
|------|------|------|
| 客户 | KH001 | 客户A |
| 客户 | KH002 | 客户B |
| 收入类别 | SR001 | 服务收入 |
| 收入类别 | SR002 | 产品销售 |
| 收款账户 | ZH001 | 工商银行 |
| 收款账户 | ZH002 | 支付宝 |
